# Credentials — .env holds the DB password and PLC addresses. Never commit it.
.env
.env.*
!.env.example

# Runtime state
*.log
spool/
data/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Frontend — app.css SI se commitea (el runtime no necesita node); esto no.
node_modules/
package-lock.json

# Django — collectstatic lo regenera en cada build.
web/staticfiles/
